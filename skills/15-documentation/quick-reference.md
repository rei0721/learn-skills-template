# 文档更新快速参考卡片

> 快速查找代码变更对应的文档更新要求

## 🔴 P0 - 必须立即更新

| 代码变更                         | 更新文档                                          | 检查点                     |
| -------------------------------- | ------------------------------------------------- | -------------------------- |
| 新增模块 `internal/xxx/`         | `project-structure.md`, `architecture.md`         | 模块说明、依赖图           |
| 删除模块                         | `project-structure.md`, `architecture.md`         | 移除说明、更新依赖图       |
| 修改 `configs/config.yaml`       | `configuration.md`                                | 配置项说明、示例           |
| 新增配置项                       | `configuration.md`                                | 配置项说明、默认值、示例   |
| 新增 API `internal/handler/`     | 创建/更新 `api/` 目录                             | 接口说明、请求/响应示例    |
| 修改 API 接口                    | 更新 `api/` 目录                                  | 更新接口说明               |
| 修改表结构 `internal/models/`    | 创建/更新 `development/database.md`               | 表结构、字段说明           |
| 修改依赖 `go.mod`                | `tech-stack.md`, `prerequisites.md`               | 依赖版本、环境要求         |
| 修改启动流程                     | `quickstart.md`, `installation.md`                | 启动步骤                   |
| 修改环境要求                     | `prerequisites.md`                                | 环境要求                   |

## 🟠 P1 - 应该尽快更新

| 代码变更                         | 更新文档                                          | 检查点                     |
| -------------------------------- | ------------------------------------------------- | -------------------------- |
| 新增功能                         | `changelog.md`, 相关功能文档                      | 变更记录、功能说明         |
| 修改现有功能                     | `changelog.md`, 相关功能文档                      | 变更记录、功能说明         |
| 性能优化                         | `changelog.md`, `architecture.md`                 | 优化说明                   |
| 安全修复                         | `changelog.md`                                    | 修复说明                   |
| Bug 修复                         | `changelog.md`, `faq.md`（常见问题）             | 修复说明                   |

## 🟡 P2 - 建议更新

| 代码变更                         | 更新文档                                          | 检查点                     |
| -------------------------------- | ------------------------------------------------- | -------------------------- |
| 代码重构（影响架构）             | `architecture.md`, `project-structure.md`         | 架构说明、结构说明         |
| 代码格式化                       | 无需更新                                          | -                          |
| 添加注释                         | 无需更新                                          | -                          |

## 📋 快速检查清单

### 提交前 3 步检查

```bash
# 1. 确认变更类型
我修改了什么？
- [ ] 模块/包
- [ ] 配置文件
- [ ] API 接口
- [ ] 数据库表
- [ ] 依赖
- [ ] 其他

# 2. 查找对应文档
根据上表找到需要更新的文档

# 3. 更新并验证
- [ ] 更新文档内容
- [ ] 验证示例可运行
- [ ] 检查格式规范
```

## 🚨 常见错误

### ❌ 错误 1: 新增配置项但未更新文档

```yaml
# configs/config.yaml
new_feature:
  enabled: true  # ❌ 新增配置项
```

**必须更新**: `docs/development/configuration.md`

### ❌ 错误 2: 新增 API 但未写文档

```go
// internal/handler/user_handler.go
func (h *UserHandler) NewAPI(c *gin.Context) {  // ❌ 新增 API
    // ...
}
```

**必须更新**: `docs/api/` 目录

### ❌ 错误 3: 修改表结构但未更新文档

```go
// internal/models/db_user.go
type User struct {
    BaseModel
    NewField string  // ❌ 新增字段
}
```

**必须更新**: `docs/development/database.md`

## 💡 提示

- **优先级 P0**: 不更新文档 = 不能提交代码
- **优先级 P1**: 应该在同一个 PR 中更新
- **优先级 P2**: 可以单独 PR 更新

---

**快速链接**:
- 完整规范: [SKILL.md](./SKILL.md)
- 文档目录: [../../docs/](../../docs/)
- 变更日志: [../../docs/changelog.md](../../docs/changelog.md)
